package com.ceiec.graph.test

import java.lang

import com.alibaba.fastjson.{JSON, JSONObject}

object TestForProcess {

  def main(args: Array[String]): Unit = {
//    f =>
//      //单条数据读取
//    {
//      if (!(f.startsWith("[")||f.endsWith("]")))
//      {
//        if (f.endsWith(",")){
//          //获取每个json数据
//          var s = f.substring(0,f.length-2)
//
//          //解析每个json数据
//          //                var message: JSONObject = JSON.parseObject(s.toString())
//
//          //获取想要的数据
//          println(s.getClass)
//        }
//      }
//
//      lang.Thread.sleep(5000)
//    }
  }
}
